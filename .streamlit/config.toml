# .streamlit/config.toml

[server]
# Forces the cloud system to send files uncompressed, preventing proxy blocks
enableWebsocketCompression = false 
maxUploadSize = 50

[browser]
# Stops tracking scripts from interfering with multipart headers
gatherUsageStats = false
